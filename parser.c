#include "holberton.h"
#include <stdarg.h>

/**
 * parser - This function parses through the format string
 * @format - The string to parse through
 * Return:
 */

char *parser(const char *format)
{
	int i;

	for (i = 0; format[i] != '\0'; i++)
	{
		while(
}
